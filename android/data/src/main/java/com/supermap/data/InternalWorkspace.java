package com.supermap.data;

import com.supermap.data.Workspace;

class InternalWorkspace extends Workspace {
	protected static void refresh(Workspace ws) {
//		Workspace.refresh(ws);
//		InternalHandleDisposable.makeSureNativeObjectLive(ws);
	}
}
